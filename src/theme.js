import {createMuiTheme} from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
        primary : {
            main: '#ff6400',
        },
        secondary: {
            main: '#006fff'
        },
        bg : {
            main: '#FFFFFF' 
        },
        text : {
            main: '#252525'
        }
    }
})

export default theme